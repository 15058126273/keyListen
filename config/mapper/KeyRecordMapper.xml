<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.play.mapper.KeyRecordMapper">

	<sql id="table_name" > key_record </sql>

	<parameterMap id="parameterType" type="KeyRecord" />

    <resultMap id="resultList" type="KeyRecord">
        <id column="id" property="id" jdbcType="BIGINT"/>
		<result column="user" property="user" jdbcType="VARCHAR"/>
		<result column="time" property="time" jdbcType="TIMESTAMP"/>
		<result column="beat_num" property="beatNum" jdbcType="INTEGER"/>
    </resultMap>

	<insert id="save" parameterMap="parameterType" useGeneratedKeys="true" keyColumn="id" keyProperty="id">
		insert into <include refid="table_name"/>
			(id, user, time, beat_num)
		values(
			#{id}, #{user}, #{time}, #{beatNum}
		)
	</insert>

</mapper>